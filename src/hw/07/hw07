Написать свою реализацию перегруженного метода subString(), который возвращает подстроку
из входящей строки.

Первая реализация метода возвращает подстроку между заданными индексами начала и конца.

Например:

String s = "Hello";
System.out.println(subString(1, 3));

Результат: "ell"

Первая реализация метода возвращает подстроку от заданного индекса начала и до конца строки.

Например:

String s = "Hello";
System.out.println(subString(1));

Результат: "ello"


